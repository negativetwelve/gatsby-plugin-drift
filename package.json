{
  "name": "gatsby-plugin-drift",
  "description": "Gatsby plugin for drift.com.",
  "version": "1.0.0",
  "author": "Mark Miyashita <negativetwelve@gmail.com>",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-react": "^6.24.1",
    "cross-env": "^5.0.5"
  },
  "keywords": [
    "drift",
    "gatsby",
    "gatsby-plugin"
  ],
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "build": "babel src --out-dir . --ignore __tests__",
    "watch": "babel -w src --out-dir . --ignore __tests__",
    "prepublish": "cross-env NODE_ENV=production npm run build"
  },
  "dependencies": {
    "babel-runtime": "^6.26.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/negativetwelve/gatsby-plugin-drift.git"
  }
}
